name: OpenWrt Hickory DNS (via Just)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # 1. Install Zig
    - uses: goto-bus-stop/setup-zig@v2
      with:
        version: '0.13.0'

    # 2. Install Tools
    - uses: taiki-e/install-action@just
    - uses: taiki-e/install-action@v2
      with:
        tool: cargo-workspaces

    # 3. Add Rust Target
    - run: rustup target add x86_64-unknown-linux-musl

    # 4. Create SMART Wrapper Scripts
    # This wrapper strips out the incorrect target flag from cc-rs
    - name: Create Zig CC Wrappers
      run: |
        mkdir -p tools
        
        # Create wrapper for CC (C Compiler)
        cat << 'EOF' > tools/x86_64-linux-musl-gcc
        #!/bin/sh
        # Filter out the incorrect target flag if present
        args=""
        for arg in "$@"; do
            if [ "$arg" = "--target=x86_64-unknown-linux-musl" ]; then
                continue
            fi
            args="$args "$arg""
        done
        # Run Zig with the correct target
        eval "zig cc -target x86_64-linux-musl $args"
        EOF
        chmod +x tools/x86_64-linux-musl-gcc
        
        # Create wrapper for CXX (C++ Compiler)
        cat << 'EOF' > tools/x86_64-linux-musl-g++
        #!/bin/sh
        args=""
        for arg in "$@"; do
            if [ "$arg" = "--target=x86_64-unknown-linux-musl" ]; then
                continue
            fi
            args="$args "$arg""
        done
        eval "zig c++ -target x86_64-linux-musl $args"
        EOF
        chmod +x tools/x86_64-linux-musl-g++
        
        # Add 'tools' to PATH
        echo "$PWD/tools" >> $GITHUB_PATH

    # 5. Build with Just
    - name: Build with Just
      env:
        CARGO_BUILD_TARGET: x86_64-unknown-linux-musl
        CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_LINKER: "zig"
        CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_RUSTFLAGS: "-C target-feature=+crt-static -C linker=zig -C link-arg=-cc -C link-arg=-target -C link-arg=x86_64-linux-musl"
        
        # Use our smart wrappers
        CC_x86_64_unknown_linux_musl: "x86_64-linux-musl-gcc"
        CXX_x86_64_unknown_linux_musl: "x86_64-linux-musl-g++"
        AR_x86_64_unknown_linux_musl: "zig ar"
      
      run: just all-features

    # 6. Prepare Artifacts
    - name: Prepare Artifacts
      run: |
        mkdir -p staging
        BINARY_NAME="hickory-dns"
        
        SOURCE=$(find target -name "$BINARY_NAME" -type f | grep "x86_64-unknown-linux-musl" | head -n 1)
        
        if [ -z "$SOURCE" ]; then 
            echo "Error: Binary '$BINARY_NAME' not found."
            exit 1
        fi
        
        echo "Found binary at: $SOURCE"
        file "$SOURCE"
        strip "$SOURCE"
        cp "$SOURCE" staging/
        
        ls -lh staging/

    # 7. Upload
    - name: Upload OpenWrt Binary
      uses: actions/upload-artifact@v4
      with:
        name: hickory-dns-just-build
        path: staging/
        if-no-files-found: error
